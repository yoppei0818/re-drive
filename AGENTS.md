# Re:Drive 開発ルール

## コミュニケーション

- ユーザーへの説明、作業報告、確認事項は日本語で記述する。
- 実装に着手する前に、対象範囲と進め方を短く共有する。

## Git運用

- 作業開始時に、タスク全体を表す `codex/` プレフィックスのブランチを作成する。
- モバイルとバックエンドにまたがる同一タスクは、同じ作業ブランチで進める。
- 変更はモバイル、バックエンド、ドキュメントなどの論理単位で分けてコミットする。
- コミットメッセージは日本語で記述する。
- コミットメッセージには、既存履歴に合わせて `feat(mobile):`、`feat(api):`、`refactor:`、`docs:` などのプレフィックスを付ける。
- ユーザーの既存変更やタスクと無関係な差分を、許可なく変更またはコミットしない。

## モノレポでの進め方

- モバイルは `apps/mobile`、FastAPIは `apps/api` を対象とする。
- 複数アプリにまたがる機能では、先にAPIの入出力契約を明確にしてから各アプリを接続する。
- 段階的に実装する場合は、各段階が単独で確認可能な状態を保つ。

## 検証

- モバイル変更後は `npm --prefix apps/mobile run lint` と `npm --prefix apps/mobile run typecheck` を実行する。
- FastAPI変更後は `apps/api/.venv/bin/pytest apps/api` と `apps/api/.venv/bin/ruff check apps/api` を実行する。
- コミット前に `git diff --check` を実行し、意図したファイルだけが含まれていることを確認する。
- 実行できなかった検証がある場合は、完了報告で理由と未確認範囲を明記する。
